# what is pandas in python ?

1. pandas is a powerfull libraries for using data set in python 
2. pandas is used to manage a structured data inside of python 
3. pandas used for dataframes , time series and series structured data using python 
4. pandas is used to manage and visualized data using matplotlib and also read data of excel or CSV.

5. pandas is used to give structured data in form of excel or csv or tables using SQL 

**how to install pandas**

```
pip install pandas
or
pip show pandas
or
import pandas as pd
print(pd.__version__)

```